

class Validation
{
    // Fetch the array of objects
    // seperate the keys from rules
    // validate the keys against the rules
    // create errors object and send to interface
}

export default new Validation()