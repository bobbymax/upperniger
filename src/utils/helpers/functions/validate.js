// const display = [
//     {
//         name: 'required',
//         message : "field is required",
//     }
// ]

export const validate = (requests, rules) => {
    // normalise requests
    let errors = {}
    const values = rules.split("|") 

    // if (name && values.include("required"))
}